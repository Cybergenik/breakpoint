use std::io::prelude::*;
use std::io::BufRe

#![allow(unused)]
#![allow(unused_imports)]
fn main() {
    let a = String::from("hello");

}

pub fn get_new_buffer(i: u8, buff: String){
    let chars: Vec<i> = a.chars().collect();
    for ch in chars{
        println!("{}", ch);
    }
    return chars;
}